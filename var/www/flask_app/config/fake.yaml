---
description: Show the current quota for project
enabled: true
entry_point: src/openstack_actions.py
name: fake
groups:
  - stfc-cloud/admins
parameters:
  lib_entry_point:
    default: workflows.fake.run
    immutable: true
    type: string
  requires_openstack:
    default: true
    immutable: true
    type: boolean
  cloud_account:
    description: The clouds.yaml account to use whilst performing this action
    required: true
    type: string
    default: "dev"
    enum:
      - "dev"
      - "prod"
runner_type: python-script
